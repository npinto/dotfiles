# -----------------------------------------------------------------------------
# -- ssh keychain / ssh-agent
# -----------------------------------------------------------------------------
if [ "$(test ! -f ${HOME}/.no_keychain && command -v keychain && test -f ~/.bashrc)" ]; then
    for f in ~/.ssh/id_?sa; do
        keychain $f &> /dev/null;
    done;
    . ~/.keychain/$(hostname)-sh;
fi
